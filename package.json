{
  "name": "capacitor-react-vite-boilerplate",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite dev --host",
    "dev:android": "cap run android",
    "dev:ios": "cap run ios",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "prepare": "yarn build && cap sync",
    "postinstall": "yarn prepare"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.5.1",
    "@types/react-dom": "^18.2.17",
    "typescript": "^5.2.2",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^5.5.1",
    "@capacitor/core": "^5.5.1",
    "@capacitor/ios": "^5.5.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
